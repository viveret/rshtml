@{
    // this code is executed before every view in this folder
    // unless the view uses @viewstart null or @viewstart ""
    view_context.insert_str("Layout", "views/shared/_layout.rs".to_string());
}